{
  "name": "@forjajs/core",
  "version": "0.0.0",
  "description": "Forja core: registry-driven, feature-based Express foundation.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist",
    "test": "vitest run"
  },
  "dependencies": {
    "express": "^4",
    "@forjajs/contracts": "*"
  },
  "devDependencies": {
    "@types/express": "^4",
    "@types/node": "*",
    "vitest": "^2"
  }
}
